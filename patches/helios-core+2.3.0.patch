diff --git a/node_modules/helios-core/dist/common/distribution/DistributionFactory.js b/node_modules/helios-core/dist/common/distribution/DistributionFactory.js
index d2d927f..7d5f624 100644
--- a/node_modules/helios-core/dist/common/distribution/DistributionFactory.js
+++ b/node_modules/helios-core/dist/common/distribution/DistributionFactory.js
@@ -195,10 +195,12 @@ class HeliosModule {
             case helios_distribution_types_1.Type.Library:
             case helios_distribution_types_1.Type.Forge:
             case helios_distribution_types_1.Type.ForgeHosted:
+            case helios_distribution_types_1.Type.NeoForgeHosted:
             case helios_distribution_types_1.Type.Fabric:
             case helios_distribution_types_1.Type.LiteLoader:
                 return (0, path_1.join)(commonDir, 'libraries', relativePath);
             case helios_distribution_types_1.Type.ForgeMod:
+            case helios_distribution_types_1.Type.NeoForgeMod:
             case helios_distribution_types_1.Type.LiteMod:
                 // TODO Move to /mods/forge eventually..
                 return (0, path_1.join)(commonDir, 'modstore', relativePath);
diff --git a/node_modules/helios-core/dist/dl/AssetGaurdTransmitter.d.ts b/node_modules/helios-core/dist/dl/AssetGaurdTransmitter.d.ts
index f0c8499..34717c6 100644
--- a/node_modules/helios-core/dist/dl/AssetGaurdTransmitter.d.ts
+++ b/node_modules/helios-core/dist/dl/AssetGaurdTransmitter.d.ts
@@ -15,6 +15,6 @@ export declare class FullRepair extends BaseTransmitter {
     constructor(commonDirectory: string, instanceDirectory: string, launcherDirectory: string, serverId: string, devMode: boolean);
     receiverName(): string;
     verifyFiles(onProgress: (percent: number) => void): Promise<number>;
-    download(onProgress: (percent: number) => void): Promise<void>;
+    download(onProgress: (percent: number, transfer?: { received: number; total: number; bytesPerSecond: number }) => void): Promise<void>;
 }
 export {};
diff --git a/node_modules/helios-core/dist/dl/AssetGaurdTransmitter.js b/node_modules/helios-core/dist/dl/AssetGaurdTransmitter.js
index 554077d..db1e3ef 100644
--- a/node_modules/helios-core/dist/dl/AssetGaurdTransmitter.js
+++ b/node_modules/helios-core/dist/dl/AssetGaurdTransmitter.js
@@ -98,7 +98,7 @@ class FullRepair extends BaseTransmitter {
                 switch (message.response) {
                     case 'downloadProgress':
                         log.debug('Received download progress ' + message.percent);
-                        onProgress(message.percent);
+                        onProgress(message.percent, message);
                         break;
                     case 'downloadComplete':
                         log.info('Received download complete.');
diff --git a/node_modules/helios-core/dist/dl/distribution/DistributionIndexProcessor.js b/node_modules/helios-core/dist/dl/distribution/DistributionIndexProcessor.js
index f09b6b4..85c2738 100644
--- a/node_modules/helios-core/dist/dl/distribution/DistributionIndexProcessor.js
+++ b/node_modules/helios-core/dist/dl/distribution/DistributionIndexProcessor.js
@@ -67,7 +67,7 @@ class DistributionIndexProcessor extends IndexProcessor_1.IndexProcessor {
         if (server == null) {
             throw new AssetGuardError_1.AssetGuardError(`Invalid server id ${this.serverId}`);
         }
-        const modLoaderModule = server.modules.find(({ rawModule: { type } }) => type === helios_distribution_types_1.Type.ForgeHosted || type === helios_distribution_types_1.Type.Forge || type === helios_distribution_types_1.Type.Fabric);
+        const modLoaderModule = server.modules.find(({ rawModule: { type } }) => type === helios_distribution_types_1.Type.ForgeHosted || type === helios_distribution_types_1.Type.Forge || type === helios_distribution_types_1.Type.NeoForgeHosted || type === helios_distribution_types_1.Type.Fabric);
         if (modLoaderModule == null) {
             throw new AssetGuardError_1.AssetGuardError('No mod loader found!');
         }
diff --git a/node_modules/helios-core/dist/dl/receivers/FullRepairReceiver.d.ts b/node_modules/helios-core/dist/dl/receivers/FullRepairReceiver.d.ts
index 4f30495..0cd7ad9 100644
--- a/node_modules/helios-core/dist/dl/receivers/FullRepairReceiver.d.ts
+++ b/node_modules/helios-core/dist/dl/receivers/FullRepairReceiver.d.ts
@@ -23,6 +23,9 @@ export interface ValidateCompleteReply {
 export interface DownloadProgressReply {
     response: 'downloadProgress';
     percent: number;
+    received: number;
+    total: number;
+    bytesPerSecond: number;
 }
 export interface DownloadCompleteReply {
     response: 'downloadComplete';
diff --git a/node_modules/helios-core/dist/dl/receivers/FullRepairReceiver.js b/node_modules/helios-core/dist/dl/receivers/FullRepairReceiver.js
index 89fe157..3d91a95 100644
--- a/node_modules/helios-core/dist/dl/receivers/FullRepairReceiver.js
+++ b/node_modules/helios-core/dist/dl/receivers/FullRepairReceiver.js
@@ -94,13 +94,35 @@ class FullRepairReceiver {
         this.assets.forEach(({ id }) => log.debug(`Asset Requires Download: ${id}`));
         // Reduce load on IPC channel by sending only whole numbers.
         let currentPercent = 0;
+        const startedAt = Date.now();
+        process.send({
+            response: 'downloadProgress',
+            percent: 0,
+            received: 0,
+            total: expectedTotalSize,
+            bytesPerSecond: 0
+        });
         const receivedEach = await (0, DownloadEngine_1.downloadQueue)(this.assets, received => {
-            const nextPercent = Math.trunc((received / expectedTotalSize) * 100);
+            const nextPercent = expectedTotalSize > 0 ? Math.trunc((received / expectedTotalSize) * 100) : 100;
             if (currentPercent !== nextPercent) {
                 currentPercent = nextPercent;
-                process.send({ response: 'downloadProgress', percent: currentPercent });
+                const elapsedSeconds = Math.max((Date.now() - startedAt) / 1000, 0.1);
+                process.send({
+                    response: 'downloadProgress',
+                    percent: currentPercent,
+                    received,
+                    total: expectedTotalSize,
+                    bytesPerSecond: Math.round(received / elapsedSeconds)
+                });
             }
         });
+        process.send({
+            response: 'downloadProgress',
+            percent: 100,
+            received: expectedTotalSize,
+            total: expectedTotalSize,
+            bytesPerSecond: Math.round(expectedTotalSize / Math.max((Date.now() - startedAt) / 1000, 0.1))
+        });
         for (const asset of this.assets) {
             if (asset.size !== receivedEach[asset.id]) {
                 log.warn(`Asset ${asset.id} declared a size of ${asset.size} bytes, but ${receivedEach[asset.id]} were received!`);
